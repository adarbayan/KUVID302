package Domain;

import static org.junit.Assert.assertNull;
import static org.junit.Assert.assertThat;
import static org.hamcrest.CoreMatchers.instanceOf;
import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class MoleculeFactoryTest {
/*
	MoleculeFactory mol = new MoleculeFactory();
	int alpha_type = 0; int beta_type = 1; int sigma_type = 2; int gamma_type = 3;
	int xPos = 0, yPos = 0, length = 0, width = 0, structure = 0, fallRate = 0, L =0;
	
	@Test
	void testNull() {	
		assertNull(mol.createMolecule(5, xPos, yPos, length, width, structure, fallRate, L));
		assertNotNull(mol.createMolecule(alpha_type, xPos, yPos, length, width, structure, fallRate, L));
	}
	
	@Test
	void testType() {
		
		Molecule molAlpha = mol.createMolecule(alpha_type, xPos, yPos, length, width, structure, fallRate, L);
		assertThat(molAlpha, instanceOf(Molecule.class));
		
		Molecule molBeta = mol.createMolecule(beta_type, xPos, yPos, length, width, structure, fallRate, L);
		assertThat(molAlpha, instanceOf(Molecule.class));
		
		Molecule molSigma = mol.createMolecule(sigma_type, xPos, yPos, length, width, structure, fallRate, L);
		assertThat(molAlpha, instanceOf(Molecule.class));
		
		Molecule molGamma = mol.createMolecule(gamma_type, xPos, yPos, length, width, structure, fallRate, L);
		assertThat(molAlpha, instanceOf(Molecule.class));
		
	}
	
	@Test
	void testExpectedException() {
	  assertThrows(IllegalArgumentException.class, () -> {
	    mol.createMolecule(alpha_type, xPos, yPos, length, width, structure, -1, L);
	  });
	 
	}
	*/
}
